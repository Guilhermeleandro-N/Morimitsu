import React from 'react';
import { useContext } from 'react';
import { AuthContext } from '../context/AuthContext';
const RoleRoute = ({rolesPermitidas, children}) => {
  return (
    <div>RoleRoute</div>
  )
}

export default RoleRoute